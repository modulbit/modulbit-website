---
name: Build & Push Docker Image
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Install Docker CLI
        run: apt-get update -qq && apt-get install -y -qq docker.io

      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to Forgejo Container Registry
        run: |
          docker login \
            -u ${{ github.actor }} \
            -p ${{ secrets.FORGEJO_TOKEN }} \
            git.modulbit.eu

      - name: Build & Push image
        shell: bash
        run: |
          SHORT_SHA="${GITHUB_SHA::7}"
          IMAGE_BASE="git.modulbit.eu/$(echo '${{ github.repository }}' | tr '[:upper:]' '[:lower:]')"
          docker build \
            -t "${IMAGE_BASE}:latest" \
            -t "${IMAGE_BASE}:sha-${SHORT_SHA}" \
            .
          docker push "${IMAGE_BASE}:latest"
          docker push "${IMAGE_BASE}:sha-${SHORT_SHA}"
